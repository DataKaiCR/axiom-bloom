# Development Context

This directory is reserved for local development continuity records. Those records may contain operational notes and are intentionally excluded from the public repository.

Only this boundary document is tracked. Axiom Bloom does not read `.context/` at build time or runtime.
